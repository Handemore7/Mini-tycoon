class SettingsScene extends Phaser.Scene {
    constructor() {
        super({ key: 'SettingsScene' });
    }

    create() {
        // This scene is for future use if we need a separate settings screen
        // Currently using overlay in GameScene
    }
}